spring:
  profiles:
    active:
      - dev

---
spring:
  profiles: dev
  application:
    name: microservicecloud-config-tianya-dev

---
spring:
  profiles: test
  application:
    name: microservicecloud-config-tianya-test